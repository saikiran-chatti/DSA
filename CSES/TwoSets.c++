  #include <iostream>
  using namespace std;
  
  int main() {
    int n,tot = 0;
    cin>>n;
    
    set<int> S;
    
    int reqSum = (n * (n+1))/4;
  
    for(int i=n;i>0;i-=2) {
      if ( (tot+i) <= reqSum) {
        s.insert(i);
        tot+=i;
      }
      
    }
  
    for(int i: S) {
      if( i  )
    }
  
    
  }